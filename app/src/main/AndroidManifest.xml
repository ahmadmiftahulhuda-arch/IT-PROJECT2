<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin internet untuk Firebase -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Izin lokasi untuk cuaca real-time -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ITPROJECT2">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".LoginActivity" android:exported="false" android:windowSoftInputMode="adjustResize"/>

        <!-- Register -->
        <activity android:name=".RegisterActivity" android:exported="false" android:windowSoftInputMode="adjustResize"/>

        <!-- Reset Password In-App (Deep Link) -->
        <activity android:name=".ResetPasswordActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Tangkap domain bawaan Firebase Hosting milik project Anda -->
                <data android:scheme="https"
                      android:host="smartliving-425c0.firebaseapp.com" />
            </intent-filter>
        </activity>

        <!-- Forgot Password -->
        <activity android:name=".ForgotPasswordActivity" android:exported="false"/>

        <!-- Home -->
        <activity android:name=".MainActivity" android:exported="false"/>

        <!-- Riwayat -->
        <activity android:name=".RiwayatActivity" android:exported="false"/>

        <!-- Kontrol -->
        <activity android:name=".KontrolActivity" android:exported="false"/>

        <!-- Informasi (Cuaca Real-time) -->
        <activity android:name=".InformasiActivity" android:exported="false"/>

        <!-- Profile -->
        <activity android:name=".ProfileActivity" android:exported="false"/>

        <!-- Kelola Keluarga -->
        <activity android:name=".KelolaKeluargaActivity" android:exported="false"/>

        <!-- Notifikasi Suhu -->
        <activity android:name=".NotifikasiSuhuActivity" android:exported="false"/>

        <!-- Riwayat Aktivitas Lengkap -->
        <activity android:name=".AktivitasActivity" android:exported="false"/>

        <!-- Profile Sub-Pages -->
        <activity android:name=".DetailAkunActivity" android:exported="false"/>
        <activity android:name=".KeamananActivity" android:exported="false"/>
        <activity android:name=".AktivitasLoginActivity" android:exported="false"/>
        <activity android:name=".PusatBantuanActivity" android:exported="false"/>
    </application>

</manifest>